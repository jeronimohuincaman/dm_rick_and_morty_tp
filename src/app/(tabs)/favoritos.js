import { View, Text } from "react-native";
import { globalStyles } from "../../styles";

export default function Favoritos() {
    return (
        <View style={globalStyles.container}>
            <Text style={globalStyles.title}>Pantalla de Favoritos</Text>
        </View>
    );
}