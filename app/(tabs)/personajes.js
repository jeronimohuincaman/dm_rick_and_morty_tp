import { View, Text } from "react-native";
import { globalStyles } from "../../styles";

export default function Personajes() {
    return (
        <View style={globalStyles.container}>
            <Text style={globalStyles.title}>Pantalla de Personajes</Text>
        </View>
    );
}
